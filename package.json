{
  "name": "Portfolio",
  "scripts": {
    "frontend": "npm --prefix frontend run dev",
    "backend": "cd backend && python manage.py runserver",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "dev_full": "concurrently --raw \"npm run frontend\" \"npm run backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
